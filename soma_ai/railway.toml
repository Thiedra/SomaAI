[build]
builder = "nixpacks"
command = "python manage.py collectstatic --noinput"

[deploy]
startCommand = "gunicorn soma_ai.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"